请为「人格出逃空间站 / Personality Escape Station」生成一个互动道具的单帧图片。

道具名称：{{propName}}
道具外观描述：{{propAppearance}}
人格空间背景（仅供气质参考，不要画成场景）：{{worldVisualContext}}

## 输出契约（必须严格遵守）
- 只生成 **一个** 互动道具，不要 spritesheet，不要多版本，不要动效序列。
- 推荐 1024×1024 正方形画布。
- 道具必须完整、居中、轮廓清楚、不裁切、不贴边，距离画面边缘至少 15%。
- 道具底部中心必须接近画布水平中心，适合让程序把 sprite 底部中心锚定到地图热点坐标。
- 背景优先透明；如果模型不支持透明背景，请使用纯绿色 `#00B000`。
- 不要生成角色、手、人物、房间、地面、底座、坐标点、定位圆圈、UI、边框、文字、Logo、水印或标签。
- 不要画发光爆炸、碎片序列、状态变化序列；只保留最清晰的默认互动状态。

## 风格
- Q版像素道具，黑色幽默但温柔，适合叠加在 900×1600 竖屏地图热点上。
- 道具语义必须明确，远看也能知道是什么。
- 不要复刻任何知名 IP、品牌物、旧 WorldX 图标或版权物。
