这是未来可选的互动道具动效 prompt，不进入 MVP 默认流程。

请为「人格出逃空间站 / Personality Escape Station」生成一个正方形道具动效 spritesheet。

道具名称：{{propName}}
道具外观描述：{{propAppearance}}
人格空间背景（仅供气质参考，不要画成场景）：{{worldVisualContext}}

## 输出契约
- 只允许 `2×2` 或 `3×3` 正方形 spritesheet。
- 每格尺寸相等，不要网格线、数字、文字、Logo 或水印。
- 每格只包含同一个道具，完整居中，不裁切，不贴边。
- 背景优先透明；如果模型不支持透明背景，请使用纯绿色 `#00B000`。
- 不要生成角色、人物、房间、地面或多种不同道具。

## 动效
- 动效应是同一道具的轻微呼吸、闪烁或状态变化。
- 不要生成复杂爆炸、碎片飞散或需要精确物理解释的序列。
