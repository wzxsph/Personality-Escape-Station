请为「人格出逃空间站 / Personality Escape Station」生成一个互动 Agent 的单帧角色图片。

Agent 身份/定位：{{characterRole}}
Agent 外观重点：{{characterAppearance}}
人格空间背景（仅供气质参考，不要画成场景）：{{worldVisualContext}}
外部 IP / 作品参考：{{ipSource}}

## 输出契约（必须严格遵守）
- 只生成 **一个** 全身角色，不要 spritesheet，不要多版本，不要动作序列。
- 推荐 1024×1024 正方形画布。
- 角色必须完整全身、居中、不裁切、不贴边，轮廓距离画面边缘至少 15%。
- 角色脚底中心必须接近画布水平中心，适合让程序把 sprite 底部中心锚定到地图热点坐标。
- 背景优先透明；如果模型不支持透明背景，请使用纯绿色 `#00B000`。
- 不要画房间、地面、底座、坐标点、定位圆圈、道具堆、 UI、边框、阴影背景、文字、Logo、水印或标签。
- 不要生成主角，不要生成多个角色。

## 风格
- Q版像素互动居民，轮廓清楚，小体量，高可读性。
- 性格可以奇怪、有一点黑色幽默，但视觉上要温柔、可互动。
- 适合叠加在 900×1600 竖屏人格空间地图热点上。
- 不要复刻任何知名 IP 角色、明星、品牌形象或旧 WorldX 示例角色。

Agent 背景：{{characterBackstory}}
